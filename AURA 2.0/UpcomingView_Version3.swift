import SwiftUI

struct UpcomingView: View {
    var body: some View {
        VStack {
            Text("Upcoming View")
                .font(.title3)
                .padding(.top, 40)
            Spacer()
        }
    }
}